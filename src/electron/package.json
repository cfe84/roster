{
  "name": "roster-electron",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && mkdir -p ./dist/frontend/ && cp -r ../frontend/dist/* ./dist/frontend",
    "bnr": "npm run build && npm run start",
    "start": "electron ."
  },
  "author": "Charles Feval",
  "license": "MIT",
  "devDependencies": {
    "electron": "^7.1.5",
    "typescript": "^3.7.3"
  }
}